# AlphaDesk — predictive multi-agent stock research engine
claude-agent-sdk>=0.2.0     # all LLM calls, billed to the Claude Max subscription
alpaca-py>=0.14.0           # market data + tradable universe
yfinance>=0.2.31            # grading, fundamentals, VIX/macro context
pandas>=2.0.0               # price frames
fastapi                     # dashboard JSON API + SSE
uvicorn                     # ASGI server
python-dotenv               # .env config
python-multipart            # form parsing
mcp[cli]>=1.0.0             # MCP server integration
